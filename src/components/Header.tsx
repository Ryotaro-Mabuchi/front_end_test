import React from 'react';
import '../styles/Header.css';

const Header: React.FC = () => {
  return (
    <header className="header">
      <h1>都道府県別の人口推移グラフを表示する</h1>
    </header>
  );
};

export default Header;
